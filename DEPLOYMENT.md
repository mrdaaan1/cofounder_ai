# 🚀 Инструкция по деплою FoundersPath AI

Это пошаговое руководство для деплоя вашего приложения на бесплатный хостинг.

## Архитектура деплоя

- **Frontend (React):** Vercel - бесплатно навсегда
- **Backend (Flask + GigaChat):** Render - 750 часов/месяц бесплатно
- **Database:** Supabase - уже настроен и работает в облаке

## Часть 1: Подготовка GitHub репозитория

### Шаг 1.1: Создайте GitHub репозиторий

1. Откройте https://github.com
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите зелёную кнопку **"New"** (или перейдите на https://github.com/new)
4. Заполните форму:
   - **Repository name:** `founderspath-ai`
   - **Description:** AI-ментор для создания питч-деков стартапов
   - **Public** или **Private** - на ваш выбор
   - НЕ ставьте галочки "Add a README" или ".gitignore" (у нас уже есть)
5. Нажмите **"Create repository"**

### Шаг 1.2: Загрузите код в GitHub

Откройте терминал в папке проекта и выполните команды:

```bash
# Инициализируем git (если ещё не сделано)
git init

# Добавляем все файлы
git add .

# Создаём первый коммит
git commit -m "Initial commit: FoundersPath AI"

# Подключаем GitHub репозиторий (замените YOUR_USERNAME на ваш логин)
git remote add origin https://github.com/YOUR_USERNAME/founderspath-ai.git

# Отправляем код на GitHub
git branch -M main
git push -u origin main
```

⚠️ **Важно:** Замените `YOUR_USERNAME` на ваш логин в GitHub!

**Пример:**
Если ваш логин `john_doe`, команда будет:
```bash
git remote add origin https://github.com/john_doe/founderspath-ai.git
```

### Шаг 1.3: Проверьте загрузку

1. Обновите страницу вашего репозитория на GitHub
2. Вы должны увидеть все файлы проекта
3. Убедитесь, что есть файлы:
   - ✅ `vercel.json`
   - ✅ `render.yaml`
   - ✅ `.env.production`
   - ✅ `backend/requirements.txt` (с gunicorn)

---

## Часть 2: Деплой Backend на Render

### Шаг 2.1: Регистрация на Render

1. Откройте https://render.com/register
2. Нажмите **"Sign Up with GitHub"** (рекомендуется)
3. Разрешите Render доступ к вашим репозиториям
4. Подтвердите email (если требуется)

### Шаг 2.2: Создание Web Service

1. На главной странице Render нажмите **"New +"** (синяя кнопка справа вверху)
2. Выберите **"Web Service"**
3. В списке репозиториев найдите `founderspath-ai` и нажмите **"Connect"**
   - Если не видите репозиторий, нажмите **"Configure account"** и дайте доступ

### Шаг 2.3: Настройка сервиса

Заполните форму:

**Базовые настройки:**
- **Name:** `founderspath-backend` (или любое другое имя)
- **Region:** `Frankfurt (EU Central)` (ближе к России = быстрее)
- **Branch:** `main`
- **Root Directory:** `backend`
- **Runtime:** `Python 3`

**Build & Deploy:**
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `gunicorn server:app`

**Instance Type:**
- Выберите **"Free"** (0$/месяц)

**Environment Variables (переменные окружения):**

Нажмите **"Add Environment Variable"** и добавьте:

1. Переменная 1:
   - **Key:** `PYTHON_VERSION`
   - **Value:** `3.11.0`

2. Переменная 2:
   - **Key:** `FLASK_ENV`
   - **Value:** `production`

3. Переменная 3:
   - **Key:** `FRONTEND_URL`
   - **Value:** `https://your-app.vercel.app` (замените после деплоя frontend!)

⚠️ **Важно:** Пока оставьте `FRONTEND_URL` пустым или укажите временное значение. Мы обновим его после деплоя frontend.

### Шаг 2.4: Запуск деплоя

1. Нажмите **"Create Web Service"** внизу страницы
2. ⏳ Подождите 3-5 минут, пока Render:
   - Склонирует репозиторий
   - Установит зависимости (Flask, GigaChat, gunicorn)
   - Запустит сервер
3. Вы увидите логи в реальном времени
4. Когда появится сообщение **"Your service is live 🎉"**, backend готов!

### Шаг 2.5: Получите URL backend

1. Скопируйте URL вашего backend (вверху страницы)
2. Он будет выглядеть так: `https://founderspath-backend.onrender.com`
3. **Сохраните этот URL!** Он понадобится для настройки frontend

### Шаг 2.6: Проверьте работу backend

Откройте в браузере:
```
https://founderspath-backend.onrender.com/health
```

Вы должны увидеть:
```json
{"status": "ok", "model": "GigaChat-2"}
```

✅ Если видите это - backend работает!

⚠️ **Важно:** При первом запросе backend может "просыпаться" 30-60 секунд (бесплатный tier засыпает после 15 минут без активности).

---

## Часть 3: Деплой Frontend на Vercel

### Шаг 3.1: Регистрация на Vercel

1. Откройте https://vercel.com/signup
2. Нажмите **"Continue with GitHub"** (рекомендуется)
3. Разрешите Vercel доступ к вашим репозиториям
4. Подтвердите email (если требуется)

### Шаг 3.2: Импорт проекта

1. На главной странице Vercel нажмите **"Add New..."** → **"Project"**
2. В списке репозиториев найдите `founderspath-ai` и нажмите **"Import"**
   - Если не видите репозиторий, нажмите **"Adjust GitHub App Permissions"**

### Шаг 3.3: Настройка проекта

Vercel автоматически определит настройки, но проверьте:

**Project Settings:**
- **Framework Preset:** `Vite` (должно определиться автоматически)
- **Root Directory:** `./` (по умолчанию)
- **Build Command:** `npm run build` (автоматически)
- **Output Directory:** `dist` (автоматически)

**Environment Variables (переменные окружения):**

Нажмите **"Add Environment Variable"** и добавьте **ТРИ** переменные:

1. Переменная 1:
   - **Name:** `VITE_SUPABASE_URL`
   - **Value:** `https://ydvgstzpaoiubedtxhuv.supabase.co`

2. Переменная 2:
   - **Name:** `VITE_SUPABASE_ANON_KEY`
   - **Value:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlkdmdzdHpwYW9pdWJlZHR4aHV2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njk2MzA5MzgsImV4cCI6MjA4NTIwNjkzOH0.HbxW3ox8ADmZJqZwv3ZXVGvAwEHTPdx8zOkphvnOQdI`

3. Переменная 3:
   - **Name:** `VITE_BACKEND_URL`
   - **Value:** `https://founderspath-backend.onrender.com` (URL из Шага 2.5!)

⚠️ **Важно:** Используйте URL вашего backend из Render (Шаг 2.5)!

### Шаг 3.4: Запуск деплоя

1. Нажмите **"Deploy"**
2. ⏳ Подождите 2-3 минуты, пока Vercel:
   - Установит зависимости (npm install)
   - Соберёт проект (npm run build)
   - Задеплоит на CDN
3. Вы увидите конфетти 🎉 когда деплой завершится!

### Шаг 3.5: Получите URL frontend

1. Скопируйте URL вашего приложения
2. Он будет выглядеть так: `https://founderspath-ai.vercel.app`
3. **Сохраните этот URL!**

---

## Часть 4: Связываем Frontend и Backend

### Шаг 4.1: Обновите FRONTEND_URL в Render

1. Вернитесь в Render Dashboard: https://dashboard.render.com
2. Откройте ваш сервис `founderspath-backend`
3. Перейдите на вкладку **"Environment"** (слева)
4. Найдите переменную `FRONTEND_URL`
5. Нажмите **"Edit"** и обновите значение на URL из Vercel:
   ```
   https://founderspath-ai.vercel.app
   ```
   (замените на ваш реальный URL!)
6. Нажмите **"Save Changes"**
7. Render автоматически передеплоит backend (подождите 1-2 минуты)

---

## Часть 5: Обновите Supabase настройки

### Шаг 5.1: Обновите URL Configuration

1. Откройте Supabase Dashboard: https://app.supabase.com
2. Выберите проект `founderspath-ai`
3. Перейдите в **Authentication** → **URL Configuration**
4. **Site URL:** Измените на `https://founderspath-ai.vercel.app` (ваш Vercel URL)
5. **Redirect URLs:** Добавьте:
   ```
   https://founderspath-ai.vercel.app/**
   http://localhost:3001/**
   ```
6. Нажмите **"Save"**

### Шаг 5.2: Обновите OAuth провайдеры (если используете)

**Для Google OAuth:**

1. Откройте Google Cloud Console: https://console.cloud.google.com/apis/credentials
2. Выберите ваш OAuth Client
3. В **Authorized redirect URIs** добавьте:
   ```
   https://ydvgstzpaoiubedtxhuv.supabase.co/auth/v1/callback
   ```
4. Сохраните

**Для GitHub OAuth:**

1. Откройте GitHub Settings: https://github.com/settings/developers
2. Выберите ваше OAuth App
3. **Authorization callback URL:** Обновите на:
   ```
   https://ydvgstzpaoiubedtxhuv.supabase.co/auth/v1/callback
   ```
4. Сохраните

---

## Часть 6: Тестирование

### Шаг 6.1: Откройте приложение

1. Перейдите на ваш Vercel URL: `https://founderspath-ai.vercel.app`
2. Вы должны увидеть экран авторизации

### Шаг 6.2: Проверьте регистрацию

1. Нажмите **"Зарегистрироваться"**
2. Введите email и пароль (минимум 6 символов)
3. Проверьте email - должно прийти письмо с подтверждением
4. Перейдите по ссылке в письме
5. Войдите в систему

### Шаг 6.3: Проверьте OAuth (опционально)

1. Попробуйте **"Войти через Google"** или **"Войти через GitHub"**
2. Разрешите доступ
3. Вы должны быть авторизованы

### Шаг 6.4: Проверьте AI-чат

1. После входа автоматически создастся проект "Мой первый проект"
2. Напишите сообщение AI (например: "У меня есть идея приложения для доставки посылок")
3. ⏳ **Первый запрос может занять 30-60 секунд** (backend просыпается с Render)
4. AI должен ответить и обновить артефакт

✅ Если всё работает - поздравляю! Приложение полностью задеплоено!

### Шаг 6.5: Проверьте сохранение данных

1. Заполните пару артефактов
2. Обновите страницу (F5)
3. Данные должны сохраниться (загрузятся из Supabase)

### Шаг 6.6: Проверьте множественные проекты

1. Нажмите на название проекта (вверху справа)
2. Выберите **"Создать новый проект"**
3. Введите название
4. Заполните артефакты во втором проекте
5. Переключитесь обратно на первый проект
6. ✅ Артефакты первого проекта должны загрузиться
7. ✅ Контекст чата должен сбросится на начальное сообщение

---

## Часть 7: Автоматические деплои (CI/CD)

### Как это работает:

- **Vercel:** При каждом `git push` в ветку `main` автоматически создаётся новый деплой
- **Render:** При каждом `git push` в ветку `main` автоматически пересобирается backend

### Как обновить приложение:

1. Внесите изменения в код локально
2. Сохраните и закоммитьте:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
3. Подождите 2-3 минуты
4. Обновите страницу приложения - изменения применятся!

---

## Troubleshooting (Решение проблем)

### ❌ Backend не отвечает (500 ошибка)

**Проблема:** `Failed to fetch` или `HTTP error! status: 500`

**Решение:**
1. Откройте Render Dashboard → ваш сервис → вкладка **"Logs"**
2. Посмотрите последние ошибки
3. Частые причины:
   - GigaChat API недоступен (попробуйте позже)
   - Не установлены зависимости (проверьте `requirements.txt`)
   - Ошибка в коде (смотрите traceback в логах)

### ❌ Frontend показывает белый экран

**Проблема:** Приложение не загружается, пустая страница

**Решение:**
1. Откройте консоль браузера (F12)
2. Смотрите ошибки в Console
3. Частые причины:
   - Неправильные environment variables в Vercel (проверьте все 3)
   - CORS ошибка (обновите `FRONTEND_URL` в Render)

### ❌ AI не отвечает (долгое ожидание)

**Проблема:** Отправили сообщение, но AI не отвечает

**Решение:**
1. Откройте консоль браузера (F12) → вкладка **"Network"**
2. Найдите запрос к `/api/chat`
3. Если статус **pending** больше 1 минуты:
   - Backend засыпает на Render Free tier
   - Первый запрос может занять до 60 секунд
   - Последующие запросы будут быстрее

### ❌ OAuth не работает

**Проблема:** При клике на "Войти через Google/GitHub" ничего не происходит

**Решение:**
1. Проверьте, что в Supabase → Authentication → Providers включены Google/GitHub
2. Убедитесь, что redirect URIs совпадают:
   ```
   https://ydvgstzpaoiubedtxhuv.supabase.co/auth/v1/callback
   ```
3. В Google Cloud / GitHub OAuth проверьте, что callback URL правильный

### ❌ Данные не сохраняются

**Проблема:** После обновления страницы артефакты пропадают

**Решение:**
1. Откройте консоль браузера (F12)
2. Смотрите ошибки Supabase
3. Частые причины:
   - RLS политики не работают (проверьте `supabase-schema.sql`)
   - Неправильные API ключи в Vercel
   - Пользователь не авторизован

### ❌ Render: "Build failed"

**Проблема:** При деплое на Render появляется ошибка

**Решение:**
1. Проверьте логи сборки
2. Убедитесь, что:
   - `backend/requirements.txt` содержит все зависимости
   - Root Directory = `backend`
   - Build Command = `pip install -r requirements.txt`
   - Start Command = `gunicorn server:app`

### ❌ Vercel: "Build failed"

**Проблема:** При деплое на Vercel появляется ошибка

**Решение:**
1. Проверьте логи сборки
2. Убедитесь, что:
   - Все environment variables добавлены
   - `package.json` содержит все зависимости
   - Framework Preset = Vite

---

## Полезные ссылки

- 📖 [Документация Vercel](https://vercel.com/docs)
- 📖 [Документация Render](https://render.com/docs)
- 📖 [Документация Supabase](https://supabase.com/docs)
- 🔐 [Настройка Auth в Supabase](https://supabase.com/docs/guides/auth)
- 🚀 [CI/CD с Vercel](https://vercel.com/docs/concepts/git)

---

## Поздравляю! 🎉

Ваше приложение FoundersPath AI теперь доступно в интернете 24/7!

**Что дальше:**

1. Поделитесь ссылкой с друзьями: `https://founderspath-ai.vercel.app`
2. Продолжайте разработку - изменения будут автоматически деплоиться
3. Мониторьте использование:
   - Vercel: бесплатно навсегда
   - Render: 750 часов/месяц (хватит с запасом)
   - Supabase: 500MB БД бесплатно

**Нужна помощь?** Создайте issue на GitHub или свяжитесь с разработчиками.
